# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build client/bff`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t client/bff`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system client/bff && \
          adduser --system -G client/bff client/bff

COPY dist/apps/client/bff client/bff/
RUN chown -R client/bff:client/bff .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix client/bff --omit=dev -f install

CMD [ "node", "client/bff" ]
